#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin>>s;
    stack<char>st;
    for(int i=0;i<s.size();i++)
    {
        st.push(s[i]);
    }
    string res="";
    while(!st.empty())
    {
        res=res+st.top();
        st.pop();
    }
    cout<<res<<"\n";
}
